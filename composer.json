{
	"name": "greenpeace/planet4-base",
	"version": "0.1.0",

	"repositories": [{
		"type": "composer",
		"url": "http://picasso.local/~tobias/Greenpeace/planet4-registry/public/"
	}],

	"require": {
		"greenpeace/planet4-wordpress": "4.7.2",
		"wp-cli/wp-cli": "^1.1"
	},

	"config": {
		"secure-http": false
	},

	"extra": {
		"installer-paths": {
			"public/": ["wordpress/wordpress"],
			"plugins/{$name}/": ["type:wordpress-plugin"],
			"themes/{$name}/": ["type:wordpress-theme"]
		}
	},

	"scripts": {
		"start": "php -S 127.0.0.1:9191 -t public public/index.php",
		"config:theme": "wp theme activate",
		"config:file": "wp core config --force",
		"config:install": "wp core install",
		"config": ["@config:file", "@config:install", "@config:theme"]
	}
}
